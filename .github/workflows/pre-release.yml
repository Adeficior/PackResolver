# <meta>
#    <source>@pssbletrngle/github-meta-generator</source>
#    <version>1.0.95</version>
#    <timestamp>2026-06-21T19:19:26.857Z</timestamp>
#    <hash>a58875cc740c964f</hash>
# </meta>

name: Trigger Prerelease

on:
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: write
  actions: write

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - uses: PssbleTrngle/find-release@v1
        if: ${{ github.event_name != 'pull_request' }}
        id: action
        with:
          type: draft
          token: $

      - uses: benc-uk/workflow-dispatch@v1
        if: toJson(steps.action.outputs.matched)
        with:
          workflow: release.yml
          inputs: '{ "rc": true, "version": "${{ steps.action.outputs.tag }}-rc.${{ github.run_number }}" }'
